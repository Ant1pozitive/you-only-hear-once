import torch

def compute_f1(preds: dict, targets: dict) -> float:
    # Implement event-based F1 for SED
    # Use polyphonic metrics like PSDS
    return 0.0  # Placeholder

def compute_map(preds: dict, targets: dict, iou_thresh: float = 0.5) -> float:
    # mAP for detection
    return 0.0  # Placeholder
